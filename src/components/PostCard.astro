---
// 文章卡片组件（首页/标签页共用）；latest=true 时标记「今日」
interface Props {
  latest?: boolean;
  slug: string;
  date: string;
  title: string;
  desc: string;
  tags: string[];
}
const { latest = false, slug, date, title, desc, tags } = Astro.props;
const displayDate = date.slice(5); // MM-DD
---
<div class="timeline-item reveal" data-tags={tags.join(',')}>
  <a class="timeline-card" href={`/posts/${slug}/`}>
    <div class="timeline-meta"><span class="timeline-date">{displayDate}</span><span class="timeline-cat">每日内核日报</span>{latest && <span class="timeline-latest">今日</span>}</div>
    <h3>{title}</h3>
    <p class="timeline-excerpt">{desc}</p>
    <div class="timeline-tags">{tags.map(t => <span class="tag">{t}</span>)}</div>
  </a>
</div>
