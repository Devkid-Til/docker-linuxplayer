---
// 正文段落：卡片化长文分析
import type { ParagraphBlock } from './types';
interface Props { block: ParagraphBlock; }
const { block } = Astro.props;
---
<p class="block-card block-paragraph" set:html={block.text} />
