---
// 导语金句：卡片化，主色浅底 + 左边框
import type { HookBlock } from './types';
interface Props { block: HookBlock; }
const { block } = Astro.props;
---
<p class="block-card block-hook" set:html={block.text} />
