---
// 引用块：卡片化
import type { QuoteBlock } from './types';
interface Props { block: QuoteBlock; }
const { block } = Astro.props;
---
<blockquote class="block-card block-quote" set:html={block.text} />
