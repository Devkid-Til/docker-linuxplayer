---
// 结尾块：收尾金句 + 数据来源，居中
import type { ClosingBlock } from './types';
interface Props { block: ClosingBlock; }
const { block } = Astro.props;
---
<div class="block-closing">
  <p class="c-tagline" set:html={block.tagline} />
  <p class="c-source" set:html={block.source} />
</div>
