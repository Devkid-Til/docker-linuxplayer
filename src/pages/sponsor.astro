---
import Base from '../layouts/Base.astro';
---
<Base title="支持一下 · Linux 内核玩家" desc="如果 Linux 内核玩家的内容对你有帮助，可以通过第三方平台支持作者">
  <main class="container">
    <div class="article-head">
      <a class="back-link" href="/">← 首页</a>
      <div class="article-meta"><span class="article-date">支持</span></div>
      <h1>支持一下 ☕</h1>
      <div class="article-divider"></div>
    </div>

    <div class="guide reveal">
      <h2>为什么放这个页面</h2>
      <p>这个博客是我业余时间维护的：每天读内核邮件列表、筛选补丁、写雷达日报、运维服务器。如果内容对你有帮助，请一杯咖啡，是对这项工作最直接的肯定。</p>
      <p class="note">完全自愿，无任何权益绑定 —— 不解锁内容、不影响阅读、不附加任何会员功能。</p>
    </div>

    <div class="guide reveal placeholder">
      <h2>🚧 打赏通道筹备中</h2>
      <p>正在接入第三方创作者平台（支持微信/支付宝，不暴露双方个人账户信息），近期上线。先把这份心意记下，过两天再来看看。</p>
    </div>

    <div class="guide reveal">
      <h2>🔍 现在就能做的</h2>
      <p>不想等，或者不想花钱也完全可以：</p>
      <ul class="alt-list">
        <li>把文章转给需要的同事/朋友</li>
        <li>通过 <a href="/feedback/">意见反馈</a> 指出错误或建议</li>
        <li>公众号「Linux内核玩家」后台留言</li>
        <li>订阅 <a href="/subscribe/">RSS</a> 常回来看看</li>
      </ul>
    </div>
  </main>

  <style>
    .guide{background:var(--card);border:1px solid var(--border);border-radius:var(--radius-lg);padding:24px;margin-bottom:20px}
    .guide h2{font-size:1.125rem;margin-bottom:8px;color:var(--brand)}
    .guide p{font-size:.9375rem;color:var(--text);line-height:1.7;margin-bottom:8px}
    .guide p.note{font-size:.8125rem;color:var(--text-secondary);background:var(--bg-alt);padding:10px 14px;border-radius:var(--radius-md);border-left:3px solid var(--brand);margin-top:12px}
    .guide.placeholder{border-style:dashed}
    .alt-list{list-style:none;padding:0;margin:8px 0 0;font-size:.9375rem;color:var(--text-secondary);line-height:1.9}
    .alt-list li{padding-left:1.25rem;position:relative}
    .alt-list li::before{content:'→';position:absolute;left:0;color:var(--brand)}
    .alt-list a{color:var(--brand);text-decoration:none;border-bottom:1px dashed var(--brand-border)}
    .alt-list a:hover{border-bottom-style:solid}
  </style>
</Base>
